-- default ## Inline Comment
* GLOBAL:
    FORMAT			          = "[%datetime{%d/%M %h:%m:%s}] %level - %msg"
    ENABLED              	= true
    FILENAME            	= "./global.log"
    TO_FILE             	= true
		TO_STANDARD_OUTPUT  	= true
		MAX_LOG_FILE_SIZE   	= 2097152 ## 2MB - Comment starts with two hashes (##)
		LOG_FLUSH_THRESHOLD 	= 100 ## Flush after every 100 logs
